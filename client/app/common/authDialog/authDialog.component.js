import template from './authDialog.html';
import controller from './authDialog.controller';
import './authDialog.styl';

let authDialogComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default authDialogComponent;
